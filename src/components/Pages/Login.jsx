import React from 'react';
import LoginComponent from "../Layout/Login/LoginComponent";

const Login = () => {
    return (
        <div>
           <LoginComponent/>
        </div>
    );
};

export default Login;