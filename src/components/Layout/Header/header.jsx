import React from 'react';
import classes from "./Header.module.css"
import Nav from "./Nav";
const Header = () => {
    return (
        <header>
            <Nav/>
        </header>
    );
};

export default Header;